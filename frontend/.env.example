# Ditto Frontend Environment Variables
# Copy to .env.local for local development

# Backend API URL (used by browser and server-side)
NEXT_PUBLIC_API_URL=http://localhost:8081

# NextAuth secret (generate with: openssl rand -hex 32)
AUTH_SECRET=dev-auth-secret-change-in-production

# Public URL of the frontend (used by NextAuth for callbacks)
NEXTAUTH_URL=http://localhost:8080

# --- OAuth Providers (optional) ---
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
